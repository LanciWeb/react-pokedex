import React from 'react';
import Loader from './Loader';

const Screen = () => {
  return (
    <section id="screen">
      <div id="pokedex"></div>
      <Loader />
    </section>
  );
};

export default Screen;
